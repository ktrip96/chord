import { Elements } from '../../types';
interface ElementUpdaterProps {
    elements: Elements;
}
declare const ElementUpdater: ({ elements }: ElementUpdaterProps) => null;
export default ElementUpdater;
